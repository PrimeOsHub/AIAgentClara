# HEARTBEAT.md - Luzia

Heartbeat purpose: keep the OpenClaw control panel coherent without creating noise.

Check periodically:

- Agent registry has Clara, Luzia, Codex, and any new agents.
- Body-parts docs match current agent files.
- Prompt IDs, GPT URLs, and versions are documented without secrets.
- Open governance tasks are visible in `DREAMS.md`.

Stay quiet when:

- No registry, prompt, or safety state changed.
- A recent check already happened.
- The only update would be noise.
