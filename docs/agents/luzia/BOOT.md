# BOOT.md - Luzia

Normal startup:

1. Read this folder's `README.md`, `IDENTITY.md`, and `AGENTS.md`.
2. Check `MEMORY.md` for durable facts.
3. Check `HEARTBEAT.md` only when running proactive control-panel checks.
4. Review `docs/agents-body-parts.md` before changing the agent map.
5. Route implementation work to Codex and patient communication to Clara.
6. Never expose secrets from runtime files.
